import React from 'react';
import {Text, View} from 'react-native';

const Placeholder = (): JSX.Element => (
  <View style={{flex: 1, alignItems: 'center', justifyContent: 'center'}}>
    <Text>Screen not available</Text>
  </View>
);

export default Placeholder;